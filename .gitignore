# 1. 빌드 결과물 (실행 파일) 무시
# 모든 폴더 내의 .out 파일과 .exe 파일은 올리지 않습니다.
*.out
*.exe
*.o

# 2. 로그 및 결과 파일 무시
# logs 폴더 안의 모든 내용은 무시합니다.
logs/
*.log

# 3. 백업 파일 무시
# backup 폴더 안의 모든 내용은 무시합니다.
backup/
*.tar.gz

# 4. OS 시스템 파일 무시 (Mac/Windows)
.DS_Store
Thumbs.db

# [중요] src와 data 폴더는 공유해야 하므로 통째로 넣지 않습니다.
# 대신 src 폴더 내의 특정 파일만 무시하고 싶다면 아래와 같이 작성합니다.
# (예: student1.c는 올리고 싶지 않다면)
# src/student1.c